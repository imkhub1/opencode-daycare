{
  "$schema": "https://opencode.ai/config.json",

  "formatter": true,

  "lsp": true,

  "references": {
    "db-schema": {
      "path": "../07-DB-Schema",
      "description": "Schema de base de datos, tablas, columnas y referencias a crear, esto no está implementado en la base de datos, solo es para referencia."
    }
  },

  "mcp": {
    "playwright": {
      "command": ["npx", "-y", "@playwright/mcp@latest"],
      "enabled": true,
      "type": "local"
    },
    "supabase": {
      "enabled": true,
      "type": "remote",
      "url": "https://mcp.supabase.com/mcp?project_ref=buhfslxahextimvdfveh"
    }
  }
}
